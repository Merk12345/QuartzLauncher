# Quartz Package Format

Quartz packages use the `.quartz` extension.

A `.quartz` file is a ZIP archive with a required `quartz.json` manifest.

## Required structure

Example:

```text
MyMod.quartz
├── quartz.json
├── icon.png
├── README.md
├── CHANGELOG.md
├── assets/
└── payload/
